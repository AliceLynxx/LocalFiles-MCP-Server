# LocalFiles MCP Server Configuration

# Comma-separated list of directories that the MCP server can access
# Example: ALLOWED_DIRECTORIES=/home/user/documents,/home/user/projects
ALLOWED_DIRECTORIES=/path/to/your/directory1,/path/to/your/directory2

# Maximum file size in bytes (default: 10MB)
MAX_FILE_SIZE=10485760

# Comma-separated list of allowed file extensions
# Leave empty to allow all extensions
ALLOWED_EXTENSIONS=.txt,.md,.py,.js,.json,.yaml,.yml,.csv,.xml,.html,.css